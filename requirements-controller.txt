# Exact controller Python package pins for reproducible local Ansible runs.
# Update process: see CONTRIBUTING.md ("Dependency updates").
#
# Verified against PyPI on 2026-08-02:
# - ansible-core 2.21.2 requires Python >= 3.12
# - ansible-lint 26.6.0 requires ansible-core >= 2.16.14
# - Fedora 40+ and Fedora Atomic Desktops ship Python 3.12+

ansible-core==2.21.2
ansible-lint==26.6.0
yamllint==1.38.0
pre-commit==4.6.1
pytest==9.1.1
